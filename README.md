# EVOLVA v10.3.5 — Single-File Boot

This release replaces the chained ES-module startup path with one deferred `app.js` file. It removes dynamic imports and nested module requests that were stalling on iPhone Safari.

Deploy every file in this directory together.

Save schema: 6. Previous v10.3.4 and older saves are migrated.
